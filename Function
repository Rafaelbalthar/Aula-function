
////Ex1 - Interpretação
/*
function minhaFuncao(variavel) {
	return variavel * 5
}

console.log(minhaFuncao(2)) // Imprimirá o número 10
console.log(minhaFuncao(10)) // Imprimirá o número 50
*/
/*
a) O que vai ser impresso no console?
10 e 50
b) O que aconteceria se retirasse os dois "console.log" e simplesmente invocasse a funçao "minhaFuncao(2)" e "minhaFuncao(10)"? O que apareceria no console?
Não aconteceria nada
*/

////Ex2 - Interpretação
/*
let textoDoUsuario = prompt("Insira um texto"); 

const outraFuncao = function(texto) {
	return texto.toLowerCase().includes("cenoura") 
}

const resposta = outraFuncao(textoDoUsuario.toString());
console.log(resposta) 
console.log(textoDoUsuario) 
*/
/*
 a) Explique o que essa funçao faz e qual a sua utilidade.
 Essa função pega o texto inserido no prompt, coloca as letras todas minusculas e busca a palavra "cenoura" no texto. Se existir retornará true senao false

 b) Determine qual sera a saida no console para cada uma das 3 entradas do usuario:
 - Eu gosto de cenoura
 - CENOURA e bom pra vista
 - Cenouras crescem na terra
* true
* true
* false
*/

////Ex1 - Codigo
/*
a)
function bio (){
	let biografia= "Eu sou Caio, tenho 23 anos, moro em São Paulo e sou estudante."
	return biografia
}
let bio2 = bio()
console.log(bio2)

*/
/*
b)
function biousuario (){
	let biouse = ("Eu sou " + nome +", tenho "+ idade +" anos, moro em "+ endereco +" e sou " + profissao +".")
	return biouse
}
const nome= prompt("Insira seu nome: ")
const idade= Number(prompt("Insira sua idade:"))
const endereco= prompt("Insira seu endereco: ")
const profissao= prompt("Insira sua profissao")

console.log(biousuario(nome, idade, endereco, profissao))

*/

////Ex2 - Codigo
/*
const numero1= Number(prompt("Insira o primeiro numero:"))
const numero2= Number(prompt("Insira o segundo numero:"))

function soma(){
    let resultado = numero1 + numero2
    return resultado
}

function maiorIgual(numero1, numero2){
    let resultado = numero1 >= numero2
    return resultado
}

function par(numero1){
    let par = numero1 % 2
    let resultado = par === 0
    return resultado
}

function Maiusculo(frase){
    let tamanho = frase.length
    let fraseMaiuscula = frase.toUpperCase()
    let resultado = "Frase: " +fraseMaiuscula+ ". "+ "Tamanho: "+ tamanho +"."
    return resultado
}


console.log(soma(numero1, numero2))
console.log(maiorIgual(numero1, numero2))
console.log(par(numero1))
console.log(Maiusculo("Eu sou estudante"))

*/

////Ex3 - Codigo
/*
function soma(numero1, numero2){
    resultado = numero1 + numero2
    return resultado
}

function subtracao(numero1, numero2){
    resultado = numero1 - numero2
    return resultado
}

function multiplicacao(numero1, numero2){
    resultado = numero1 * numero2
    return resultado
}

function divisao(numero1, numero2){
    resultado = numero1 / numero2
    return resultado
}

let numero1 = Number(prompt("Insira o seu primeiro número."))
let numero2 = Number(prompt("Insira o seu segundo número."))

console.log("Numeros inseridos: "+ numero1 + " e " + numero2)
console.log("Soma: " + soma(numero1, numero2))
console.log("Diferença: " + subtracao(numero1, numero2))
console.log("Multiplicação: " + multiplicacao(numero1, numero2))
console.log("Divisão: " + divisao(numero1, numero2))

*/
